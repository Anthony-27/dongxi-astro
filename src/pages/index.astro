---
import Base from '../layouts/Base.astro';
import { getShowcase, urlFor } from '../lib/sanity';

const showcaseItems = await getShowcase();
const showcaseJSON = JSON.stringify(
  showcaseItems.map((s: any) => ({
    id:       s._id,
    title:    s.title,
    tag:      s.tag   ?? '',
    category: s.category ?? '',
    meta:     s.meta  ?? '',
    images:   (s.images ?? []).map((img: any) =>
      urlFor(img).width(900).auto('format').url()
    ),
  }))
);
---
<Base
  title="DONGXI — Guangzhou Fabric & Garment Manufacturer"
  description="Direct factory in Guangzhou. Silk, lace, embroidery, custom weaves. 48-hour quote. MOQ from 50m."
  ogUrl="https://dongxi.io"
>

<!-- NAV -->
<header class="nav" id="nav">
  <nav class="nav-left">
    <a href="#catalog" class="nav-link" data-k="nav_cat">Catalog</a>
    <a href="#showcase" class="nav-link" data-k="nav_sc">Work</a>
  </nav>
  <a href="/" class="nav-logo"><span class="nav-pip"></span>DONGXI</a>
  <nav class="nav-right">
    <a href="#inquiry" class="nav-link" data-k="nav_inq">Inquiry</a>
    <a href="/shop" class="nav-link nav-shop" data-k="nav_shop">Shop ↗</a>
    <div class="lang-wrap">
      <button class="nav-link lang-cur" id="langBtn">EN</button>
      <div class="lang-drop" id="langDrop">
        <button class="ld-item" data-l="en">English</button>
        <button class="ld-item" data-l="zh">中文</button>
        <button class="ld-item" data-l="ru">Русский</button>
        <button class="ld-item" data-l="ar">العربية</button>
      </div>
    </div>
  </nav>
  <!-- mobile -->
  <div class="nav-mobile">
    <a href="/shop" class="nav-link nav-shop">Shop ↗</a>
    <div class="lang-wrap">
      <button class="nav-link lang-cur" id="langBtnM">EN</button>
      <div class="lang-drop" id="langDropM">
        <button class="ld-item" data-l="en">English</button>
        <button class="ld-item" data-l="zh">中文</button>
        <button class="ld-item" data-l="ru">Русский</button>
        <button class="ld-item" data-l="ar">العربية</button>
      </div>
    </div>
  </div>
</header>

<!-- HERO -->
<section class="hero" id="hero">
  <div class="hero-bg"></div>
  <div class="hero-content">
    <p class="hero-eyebrow" data-k="h_label">Guangzhou · Direct Factory</p>
    <h1 class="hero-h">
      <span data-k="h_line1">Fabric</span><br>
      <em data-k="h_line2">for your</em><br>
      <span data-k="h_line3">brand.</span>
    </h1>
    <div class="hero-actions">
      <a href="#inquiry" class="btn-ghost btn-light" data-k="cta_inq">Get a Quote</a>
      <a href="/shop"    class="btn-solid-rust" data-k="cta_shop">Shop Ready Stock ↗</a>
    </div>
  </div>
  <div class="hero-foot">
    <span data-k="hero_f1">48h quote</span>
    <span class="hf-sep">·</span>
    <span data-k="hero_f2">MOQ from 50 m</span>
    <span class="hf-sep">·</span>
    <span data-k="hero_f3">Ships worldwide</span>
    <span class="hf-sep">·</span>
    <span data-k="hero_f4">12 years</span>
  </div>
</section>

<!-- CATALOG -->
<section class="catalog-sec" id="catalog">
  <div class="sec-container">
    <p class="sec-eyebrow" data-k="cat_label">What we make</p>
    <h2 class="sec-title" data-k="cat_h">Our catalog.</h2>
    <p class="sec-sub" data-k="cat_sub">Select a material to start an inquiry.</p>
    <div class="fab-grid" id="fabGrid"></div>
  </div>
</section>

<!-- SHOWCASE -->
<section class="showcase-sec" id="showcase">
  <div class="sec-container sc-header-row">
    <div>
      <p class="sec-eyebrow" data-k="sc_label">From our factory</p>
      <h2 class="sec-title" data-k="sc_h">Recent work.</h2>
    </div>
    <div class="sc-filters" id="scFilters">
      <button class="scf active" data-f="all"        data-k="scf_all">All</button>
      <button class="scf"        data-f="garment"    data-k="scf_gar">Garments</button>
      <button class="scf"        data-f="fabric"     data-k="scf_fab">Fabrics</button>
      <button class="scf"        data-f="embroidery" data-k="scf_emb">Embroidery</button>
    </div>
  </div>
  <div class="sc-track-wrap">
    <div class="sc-track" id="scTrack"></div>
  </div>
  <p class="sc-empty" id="scEmpty" style="display:none">No items yet.</p>
</section>

<!-- INQUIRY -->
<section class="inq-sec" id="inquiry">
  <div class="sec-container inq-layout">
    <div class="inq-left">
      <p class="sec-eyebrow" data-k="inq_label">Start your order</p>
      <h2 class="sec-title inq-title" data-k="inq_h">Get a quote<br><em>in 48 hours.</em></h2>
      <ul class="inq-facts">
        <li data-k="ps1d">Share photos, sketches or samples.</li>
        <li data-k="ps2d">We reply within 48 hours with options & price.</li>
        <li data-k="ps3d">Sample shipped to you before bulk production.</li>
      </ul>
    </div>
    <form id="iqForm" class="inq-form" novalidate>
      <div class="iq-row two-col">
        <div>
          <label class="iq-label" data-k="f_name">Name *</label>
          <input class="iq-in" id="fName" type="text" required autocomplete="name">
        </div>
        <div>
          <label class="iq-label" data-k="f_email">Email *</label>
          <input class="iq-in" id="fEmail" type="email" required autocomplete="email">
        </div>
      </div>
      <div class="iq-row">
        <label class="iq-label" data-k="f_type">Order type</label>
        <div class="radio-grid">
          <label class="ropt"><input type="radio" name="orderType" value="photo_to_bulk"><span data-k="fo1">Photo / sketch → bulk production</span></label>
          <label class="ropt"><input type="radio" name="orderType" value="sample_to_bulk"><span data-k="fo2">Send sample → replicate & bulk</span></label>
          <label class="ropt"><input type="radio" name="orderType" value="fabric_only"><span data-k="fo3">Source fabric only</span></label>
          <label class="ropt"><input type="radio" name="orderType" value="full_cmt"><span data-k="fo4">Full CMT — cut, make & trim</span></label>
        </div>
      </div>
      <div class="iq-row">
        <label class="iq-label" data-k="f_dest">Destination country</label>
        <select class="iq-in iq-select" id="fDest">
          <option value="">— select —</option>
          <optgroup label="Asia"><option>Japan</option><option>South Korea</option><option>Singapore</option><option>Malaysia</option><option>Thailand</option><option>Vietnam</option><option>Philippines</option><option>Indonesia</option><option>India</option><option>Bangladesh</option><option>Pakistan</option><option>China · 中国大陆</option><option>China · 中国香港 HK</option><option>China · 中国澳门 MO</option><option>China · 中国台湾 TW</option></optgroup>
          <optgroup label="Middle East"><option>UAE</option><option>Saudi Arabia</option><option>Qatar</option><option>Kuwait</option><option>Turkey</option><option>Israel</option></optgroup>
          <optgroup label="Europe"><option>United Kingdom</option><option>France</option><option>Germany</option><option>Italy</option><option>Spain</option><option>Netherlands</option><option>Sweden</option><option>Switzerland</option><option>Poland</option><option>Russia</option><option>Ukraine</option></optgroup>
          <optgroup label="Americas"><option>United States</option><option>Canada</option><option>Mexico</option><option>Brazil</option><option>Colombia</option></optgroup>
          <optgroup label="Africa"><option>South Africa</option><option>Nigeria</option><option>Morocco</option><option>Egypt</option></optgroup>
          <optgroup label="Oceania"><option>Australia</option><option>New Zealand</option></optgroup>
          <option value="Other">Other</option>
        </select>
      </div>
      <div class="iq-row">
        <label class="iq-label" data-k="f_msg">Tell us what you need</label>
        <textarea class="iq-in" id="fMsg" rows="4" placeholder="Fabric type, quantity, deadline…"></textarea>
      </div>
      <div class="iq-row">
        <label class="iq-label" data-k="f_files">Reference photos / tech pack</label>
        <label class="file-drop" id="fileDrop">
          <input type="file" id="fFiles" multiple accept="image/*,.pdf,.ai,.eps" style="display:none">
          <span class="fd-txt" data-k="fd_txt">Drop files or click to browse</span>
          <span class="fd-sub" data-k="fd_sub">JPG · PNG · PDF · AI</span>
        </label>
        <div class="file-list" id="fileList"></div>
      </div>
      <div class="iq-row iq-bottom">
        <label class="iq-gdpr">
          <input type="checkbox" id="fGdpr" required>
          <span data-k="f_gdpr">I agree to be contacted regarding my inquiry.</span>
        </label>
        <button type="submit" class="btn-ghost btn-dark" id="subBtn" data-k="btn_send">Send Inquiry</button>
      </div>
      <div id="iqMsg" class="iq-msg"></div>
    </form>
  </div>
</section>

<!-- FOOTER -->
<footer class="foot">
  <div class="sec-container foot-inner">
    <div>
      <a href="/" class="nav-logo foot-logo"><span class="nav-pip"></span>DONGXI</a>
      <p class="foot-sub" data-k="ft_tag">Guangzhou textile manufacturer since 2012.</p>
    </div>
    <div class="foot-links">
      <a href="#catalog"  data-k="nav_cat">Catalog</a>
      <a href="#showcase" data-k="nav_sc">Work</a>
      <a href="#inquiry"  data-k="nav_inq">Inquiry</a>
      <a href="/shop"     data-k="nav_shop">Shop</a>
      <a href="mailto:hello@dongxi.io">hello@dongxi.io</a>
    </div>
  </div>
  <div class="foot-copy">
    <span>© 1995–2025 DONGXI · 广州东西贸易有限公司</span>
    <span data-k="ft_rights">All rights reserved</span>
  </div>
</footer>

</Base>

<style is:global>
/* ── RESET & BASE ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
body { background: #f8f5f1; color: #1a1a1a; font-family: 'Azeret Mono', monospace; -webkit-font-smoothing: antialiased; }

/* ── TOKENS ── */
:root {
  --ink:     #1a1a1a;
  --paper:   #f8f5f1;
  --paper2:  #f2ede8;
  --rust:    #c04b28;
  --border:  #e5e0d8;
  --c-w: min(100% - 48px, 1280px);
  --sec-pad: clamp(80px, 10vw, 140px);
}

/* ── TYPOGRAPHY ── */
.editorial  { font-family: 'Cormorant Garamond', 'Libre Baskerville', Georgia, serif; font-weight: 300; }
.mono       { font-family: 'Azeret Mono', monospace; }
.zh         { font-family: 'Noto Serif SC', serif; }

/* ── LAYOUT ── */
.sec-container { width: var(--c-w); margin-inline: auto; }

/* ── NAV ── */
.nav {
  position: fixed; inset-block-start: 0; inset-inline: 0; z-index: 100;
  height: 60px;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding-inline: clamp(20px, 4vw, 60px);
  background: transparent;
  transition: background .4s ease, border-color .4s ease;
  border-bottom: 1px solid transparent;
}
.nav.scrolled { background: #f8f5f1; border-bottom-color: var(--border); }
.nav.on-hero  { color: #f8f5f1; }
.nav-left  { display: flex; align-items: center; gap: 32px; }
.nav-right { display: flex; align-items: center; gap: 24px; justify-content: flex-end; }
.nav-logo {
  font-family: 'Azeret Mono', monospace;
  font-size: 13px; letter-spacing: .2em;
  text-decoration: none; color: inherit;
  display: flex; align-items: center; gap: 8px;
  justify-content: center;
}
.nav-pip { width: 6px; height: 6px; border-radius: 50%; background: var(--rust); flex-shrink: 0; }
.nav-link {
  font-family: 'Azeret Mono', monospace;
  font-size: 10px; letter-spacing: .14em; text-transform: uppercase;
  text-decoration: none; color: inherit; background: none; border: none;
  cursor: pointer; transition: opacity .15s;
}
.nav-link:hover { opacity: .5; }
.nav-shop { opacity: .65; }
.nav-mobile { display: none; align-items: center; gap: 16px; grid-column: 3; justify-content: flex-end; }
@media(max-width: 700px) {
  .nav { grid-template-columns: auto 1fr; }
  .nav-left, .nav-right { display: none; }
  .nav-mobile { display: flex; }
  .nav-logo { justify-content: flex-start; }
}

/* Lang dropdown */
.lang-wrap { position: relative; }
.lang-drop {
  display: none; position: absolute; top: calc(100% + 10px); right: 0;
  background: var(--ink); border: none;
  padding: 8px 0; min-width: 110px; z-index: 200;
}
.lang-drop.open { display: block; }
.ld-item {
  display: block; width: 100%; text-align: left;
  padding: 9px 16px;
  font-family: 'Azeret Mono', monospace; font-size: 9px; letter-spacing: .1em;
  color: rgba(248,245,241,.5); background: none; border: none; cursor: pointer;
  transition: color .1s;
}
.ld-item:hover { color: #f8f5f1; }

/* ── BUTTONS ── */
.btn-ghost {
  display: inline-block;
  font-family: 'Azeret Mono', monospace;
  font-size: 10px; letter-spacing: .16em; text-transform: uppercase;
  text-decoration: none;
  padding: 14px 36px;
  border: 1px solid currentColor;
  background: transparent; color: inherit;
  transition: background .25s, color .25s;
  cursor: pointer; border-radius: 0;
  white-space: nowrap;
}
.btn-ghost:hover { background: currentColor; }
.btn-light { color: #f8f5f1; }
.btn-light:hover { color: #1a1a1a; }
.btn-ghost-dim { opacity: .55; }
.btn-ghost-dim:hover { opacity: 1; }
.btn-dark { color: #1a1a1a; }
.btn-dark:hover { color: #f8f5f1; }
.btn-solid-rust {
  display: inline-block;
  font-family: 'Azeret Mono', monospace;
  font-size: 10px; letter-spacing: .16em; text-transform: uppercase;
  text-decoration: none;
  padding: 14px 36px;
  background: var(--rust); color: #f8f5f1;
  border: 1px solid var(--rust);
  transition: background .25s, border-color .25s;
  cursor: pointer; border-radius: 0; white-space: nowrap;
}
.btn-solid-rust:hover { background: #a03d20; border-color: #a03d20; }

/* ── HERO ── */
.hero {
  position: relative;
  height: 100svh; min-height: 600px;
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  overflow: hidden;
  color: #f8f5f1;
}
.hero-bg {
  position: absolute; inset: 0;
  background: var(--ink);
  /* when you have a hero image, replace with:
     background: url('...') center/cover no-repeat;
     then add: after { content:''; position:absolute; inset:0; background:rgba(0,0,0,.35); } */
}
.hero-content {
  position: relative; z-index: 1;
  text-align: center;
  padding-inline: 24px;
}
.hero-eyebrow {
  font-family: 'Azeret Mono', monospace;
  font-size: 10px; letter-spacing: .22em; text-transform: uppercase;
  color: rgba(248,245,241,.45);
  margin-bottom: 32px;
}
.hero-h {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 300;
  font-size: clamp(72px, 13vw, 160px);
  line-height: 1.0;
  letter-spacing: -.01em;
  margin-bottom: 52px;
}
.hero-h em { font-style: italic; color: rgba(248,245,241,.7); }
.hero-actions { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
.hero-foot {
  position: absolute; bottom: 32px; left: 50%; transform: translateX(-50%);
  z-index: 1;
  display: flex; align-items: center; gap: 20px;
  font-family: 'Azeret Mono', monospace;
  font-size: 9px; letter-spacing: .14em; text-transform: uppercase;
  color: rgba(248,245,241,.28);
  white-space: nowrap;
}
.hf-sep { opacity: .3; }
@media(max-width: 560px) {
  .hero-foot { gap: 12px; font-size: 8px; flex-wrap: wrap; justify-content: center; }
}

/* ── SECTION SHARED ── */
.sec-eyebrow {
  font-family: 'Azeret Mono', monospace;
  font-size: 9px; letter-spacing: .2em; text-transform: uppercase;
  color: var(--rust); margin-bottom: 16px;
}
.sec-title {
  font-family: 'Cormorant Garamond', Georgia, serif;
  font-weight: 300;
  font-size: clamp(36px, 5vw, 64px);
  line-height: 1.1; letter-spacing: -.01em;
  color: var(--ink);
}
.sec-title em { font-style: italic; color: var(--rust); }
.sec-sub {
  font-family: 'Azeret Mono', monospace;
  font-size: 10px; color: rgba(26,26,26,.4);
  margin-top: 10px; letter-spacing: .04em;
}

/* ── CATALOG ── */
.catalog-sec {
  padding: var(--sec-pad) 0;
  border-top: 1px solid var(--border);
}
.fab-grid {
  display: flex; flex-wrap: wrap; gap: 10px;
  margin-top: 40px;
}
.fb {
  font-family: 'Azeret Mono', monospace;
  font-size: 9px; letter-spacing: .1em; text-transform: uppercase;
  padding: 10px 22px;
  border: 1px solid var(--border);
  background: transparent; color: rgba(26,26,26,.5);
  cursor: pointer; transition: border-color .2s, color .2s, background .2s;
  border-radius: 0;
}
.fb:hover {
  border-color: var(--ink); color: var(--paper);
  background: var(--ink);
}

/* ── SHOWCASE ── */
.showcase-sec {
  padding: var(--sec-pad) 0;
  border-top: 1px solid var(--border);
  overflow: hidden;
}
.sc-header-row {
  display: flex; align-items: flex-end;
  justify-content: space-between; gap: 24px;
  flex-wrap: wrap; margin-bottom: 48px;
}
.sc-filters { display: flex; gap: 0; }
.scf {
  font-family: 'Azeret Mono', monospace;
  font-size: 9px; letter-spacing: .12em; text-transform: uppercase;
  padding: 10px 20px;
  background: transparent; border: 1px solid var(--border);
  margin-left: -1px;
  color: rgba(26,26,26,.4); cursor: pointer; transition: all .15s;
}
.scf:first-child { margin-left: 0; }
.scf.active, .scf:hover { background: var(--ink); border-color: var(--ink); color: var(--paper); z-index: 1; }
.sc-track-wrap {
  padding-left: calc((100% - var(--c-w)) / 2);
  overflow-x: auto; scrollbar-width: none; cursor: grab;
}
.sc-track-wrap::-webkit-scrollbar { display: none; }
.sc-track { display: flex; gap: 2px; width: max-content; padding-right: 48px; }
.sc-card { width: 320px; flex: 0 0 320px; overflow: hidden; background: var(--paper2); }
@media(max-width: 600px) { .sc-card { width: 260px; flex: 0 0 260px; } }
.sc-card-media { position: relative; aspect-ratio: 3/4; overflow: hidden; }
.sc-slides { display:flex; width:100%; height:100%; overflow-x:scroll; scroll-snap-type:x mandatory; scrollbar-width:none; }
.sc-slides::-webkit-scrollbar { display:none; }
.sc-slide { flex:0 0 100%; scroll-snap-align:start; }
.sc-slide img { width:100%; height:100%; object-fit:cover; display:block; transition: transform .6s ease; }
.sc-card:hover .sc-slide img { transform: scale(1.03); }
.sc-dots { position:absolute; bottom:16px; left:50%; transform:translateX(-50%); display:flex; gap:5px; pointer-events:none; }
.sc-dot { width:4px; height:4px; border-radius:50%; background:rgba(248,245,241,.35); transition:background .2s; }
.sc-dot.on { background:#f8f5f1; }
.sc-info { padding: 20px 20px 24px; }
.sc-tag { font-family:'Azeret Mono',monospace; font-size:8px; letter-spacing:.14em; text-transform:uppercase; color:var(--rust); }
.sc-title { font-family:'Cormorant Garamond',serif; font-weight:300; font-size:20px; margin-top:6px; line-height:1.25; color:var(--ink); }
.sc-meta { font-family:'Azeret Mono',monospace; font-size:9px; color:rgba(26,26,26,.38); margin-top:5px; }
.sc-empty { font-family:'Azeret Mono',monospace; font-size:10px; color:rgba(26,26,26,.3); padding:60px 0; width:var(--c-w); margin-inline:auto; }

/* ── INQUIRY ── */
.inq-sec {
  padding: var(--sec-pad) 0;
  background: var(--ink); color: var(--paper);
  border-top: 1px solid rgba(248,245,241,.06);
}
.inq-layout {
  display: grid;
  grid-template-columns: 1fr 1.5fr;
  gap: 100px; align-items: start;
}
@media(max-width: 860px) { .inq-layout { grid-template-columns: 1fr; gap: 56px; } }
.inq-left { padding-top: 4px; }
.inq-title { color: var(--paper); }
.inq-facts {
  list-style: none;
  margin-top: 40px;
  display: flex; flex-direction: column; gap: 16px;
}
.inq-facts li {
  font-family: 'Azeret Mono', monospace;
  font-size: 10px; letter-spacing: .04em; line-height: 1.9;
  color: rgba(248,245,241,.35);
  padding-left: 18px; position: relative;
}
.inq-facts li::before {
  content: '—';
  position: absolute; left: 0;
  color: var(--rust);
}
.inq-form { display: flex; flex-direction: column; gap: 28px; }
.iq-row { display: flex; flex-direction: column; gap: 10px; }
.iq-row.two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
@media(max-width: 500px) { .iq-row.two-col { grid-template-columns: 1fr; } }
.iq-label {
  font-family: 'Azeret Mono', monospace;
  font-size: 9px; letter-spacing: .12em; text-transform: uppercase;
  color: rgba(248,245,241,.35);
}
.iq-in {
  width: 100%; padding: 14px 0;
  font-family: 'Azeret Mono', monospace; font-size: 11px;
  color: var(--paper); background: transparent;
  border: none; border-bottom: 1px solid rgba(248,245,241,.15);
  outline: none; transition: border-color .2s;
  box-sizing: border-box;
}
.iq-in::placeholder { color: rgba(248,245,241,.2); }
.iq-in:focus { border-bottom-color: rgba(248,245,241,.5); }
textarea.iq-in { resize: none; line-height: 1.8; }
.file-drop {
  display: flex; flex-direction: column; gap: 4px;
  border: 1px dashed rgba(248,245,241,.15);
  padding: 28px 24px; text-align: center;
  cursor: pointer; transition: border-color .2s;
}
.file-drop:hover { border-color: rgba(248,245,241,.35); }
.fd-txt { font-family:'Azeret Mono',monospace; font-size:10px; color:rgba(248,245,241,.4); }
.fd-sub { font-family:'Azeret Mono',monospace; font-size:8px; color:rgba(248,245,241,.2); letter-spacing:.06em; }
.file-list { display:flex; flex-wrap:wrap; gap:8px; margin-top:4px; }
.fi-chip { font-family:'Azeret Mono',monospace; font-size:9px; padding:5px 12px; border:1px solid rgba(248,245,241,.15); color:rgba(248,245,241,.5); }
.iq-bottom { flex-direction: row; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 20px; }
.iq-gdpr { display:flex; align-items:flex-start; gap:10px; font-family:'Azeret Mono',monospace; font-size:9px; letter-spacing:.04em; color:rgba(248,245,241,.3); cursor:pointer; line-height:1.7; }
.iq-gdpr input { accent-color:var(--rust); margin-top:3px; flex-shrink:0; }
.radio-grid { display:grid; grid-template-columns:1fr 1fr; gap:10px 24px; padding-top:4px; }
@media(max-width:560px){ .radio-grid { grid-template-columns:1fr; } }
.ropt { display:flex; align-items:flex-start; gap:10px; font-family:'Azeret Mono',monospace; font-size:9px; letter-spacing:.04em; color:rgba(248,245,241,.45); cursor:pointer; line-height:1.7; }
.ropt input { accent-color:var(--rust); margin-top:3px; flex-shrink:0; }
.iq-select { cursor:pointer; }
.iq-select option, .iq-select optgroup { background:#1a1a1a; color:#f8f5f1; }
.iq-msg { font-family:'Azeret Mono',monospace; font-size:10px; padding:12px 16px; display:none; }
.iq-msg.ok  { background:rgba(60,140,60,.12); color:#7ecf7e; display:block; }
.iq-msg.err { background:rgba(200,60,60,.1);  color:#e08080; display:block; }

/* ── FOOTER ── */
.foot {
  background: var(--ink); color: var(--paper);
  border-top: 1px solid rgba(248,245,241,.06);
  padding: 60px 0 0;
}
.foot-inner {
  display: flex; justify-content: space-between;
  align-items: flex-start; gap: 40px; flex-wrap: wrap;
}
.foot-logo { color: var(--paper); }
.foot-sub { font-family:'Azeret Mono',monospace; font-size:9px; color:rgba(248,245,241,.22); margin-top:12px; line-height:1.8; }
.foot-links { display:flex; gap:24px; flex-wrap:wrap; align-items:center; }
.foot-links a { font-family:'Azeret Mono',monospace; font-size:9px; letter-spacing:.1em; text-transform:uppercase; color:rgba(248,245,241,.3); text-decoration:none; transition:color .15s; }
.foot-links a:hover { color:var(--paper); }
.foot-copy {
  display:flex; justify-content:space-between; align-items:center;
  border-top:1px solid rgba(248,245,241,.05);
  margin-top:48px; padding:18px 0;
  width:var(--c-w); margin-inline:auto;
}
.foot-copy span { font-family:'Azeret Mono',monospace; font-size:8px; letter-spacing:.06em; color:rgba(248,245,241,.15); }
</style>

<script define:vars={{ showcaseJSON }}>
const T = {
  en: {
    nav_cat:'Catalog', nav_sc:'Work', nav_inq:'Inquiry', nav_shop:'Shop ↗',
    h_label:'Guangzhou · Direct Factory',
    h_line1:'Your Vision,', h_line2:'Fully', h_line3:'Fabricated.',
    cta_inq:'Get a Quote', cta_shop:'Shop Ready Stock ↗',
    hero_f1:'48h quote', hero_f2:'MOQ from 50 m', hero_f3:'Ships worldwide', hero_f4:'Est. 1995',
    cat_label:'What we make', cat_h:'Our catalog.',
    cat_sub:'Select a material to start an inquiry.',
    sc_label:'From our factory', sc_h:'Recent work.',
    scf_all:'All', scf_gar:'Garments', scf_fab:'Fabrics', scf_emb:'Embroidery',
    inq_label:'Start your order', inq_h:'Get a quote\nin 48 hours.',
    ps1d:'Share photos, sketches or samples.',
    ps2d:'We reply within 48 hours with options & price.',
    ps3d:'Sample shipped to you before bulk production.',
    f_name:'Name *', f_email:'Email *',
    f_type:'Order type', f_dest:'Destination country',
    fo1:'Photo / sketch → bulk production', fo2:'Send sample → replicate & bulk',
    fo3:'Source fabric only', fo4:'Full CMT — cut, make & trim',
    f_msg:'Tell us what you need',
    f_files:'Reference photos / tech pack',
    fd_txt:'Drop files or click to browse', fd_sub:'JPG · PNG · PDF · AI',
    f_gdpr:'I agree to be contacted regarding my inquiry.',
    btn_send:'Send Inquiry',
    ft_tag:'Guangzhou textile manufacturer since 1995.',
    ft_rights:'All rights reserved',
    msg_ok:'✓ Sent. We\'ll reply within 48 hours.',
    msg_err:'Something went wrong — email hello@dongxi.io',
    msg_val:'Please fill in your name and email.',
  },
  zh: {
    nav_cat:'产品目录', nav_sc:'作品', nav_inq:'询价', nav_shop:'购买 ↗',
    h_label:'广州 · 直接工厂',
    h_line1:'您的愿景，', h_line2:'我们', h_line3:'完整呈现。',
    cta_inq:'立即询价', cta_shop:'购买现货 ↗',
    hero_f1:'48小时报价', hero_f2:'最低起订50米', hero_f3:'全球发货', hero_f4:'创立于1995年',
    cat_label:'我们的产品', cat_h:'产品目录。',
    cat_sub:'点击面料类型，开始询价。',
    sc_label:'工厂出品', sc_h:'近期作品。',
    scf_all:'全部', scf_gar:'成衣', scf_fab:'面料', scf_emb:'刺绣',
    inq_label:'开始您的订单', inq_h:'48小时内\n获得报价。',
    ps1d:'分享您的照片、草图或样品。',
    ps2d:'我们48小时内回复面料方案及报价。',
    ps3d:'实物样品寄送确认后再大货生产。',
    f_name:'姓名 *', f_email:'邮箱 *',
    f_type:'订单类型', f_dest:'目的地国家',
    fo1:'来图/草图 → 批量生产', fo2:'寄样衣 → 复制并大货',
    fo3:'仅采购面料', fo4:'全程CMT — 裁剪、缝制、修整',
    f_msg:'告诉我们您的需求',
    f_files:'参考图片 / 技术包',
    fd_txt:'拖拽文件或点击选择', fd_sub:'JPG · PNG · PDF · AI',
    f_gdpr:'我同意就本次询价接受联系。',
    btn_send:'发送询价',
    ft_tag:'广州纺织制造商，始于1995年。',
    ft_rights:'版权所有',
    msg_ok:'✓ 已发送！我们将在48小时内回复。',
    msg_err:'出现问题，请发邮件至 hello@dongxi.io',
    msg_val:'请填写姓名和邮箱。',
  },
  ru: {
    nav_cat:'Каталог', nav_sc:'Работы', nav_inq:'Запрос', nav_shop:'Магазин ↗',
    h_label:'Гуанчжоу · Прямой производитель',
    h_line1:'Ваше видение,', h_line2:'воплощённое', h_line3:'полностью.',
    cta_inq:'Получить цену', cta_shop:'Купить сейчас ↗',
    hero_f1:'Цена за 48 ч', hero_f2:'МОЗ от 50 м', hero_f3:'Доставка по миру', hero_f4:'Осн. в 1995',
    cat_label:'Что мы производим', cat_h:'Наш каталог.',
    cat_sub:'Выберите материал, чтобы начать запрос.',
    sc_label:'С нашей фабрики', sc_h:'Последние работы.',
    scf_all:'Все', scf_gar:'Одежда', scf_fab:'Ткани', scf_emb:'Вышивка',
    inq_label:'Начать заказ', inq_h:'Получите цену\nза 48 часов.',
    ps1d:'Поделитесь фото, эскизами или образцами.',
    ps2d:'Ответим в течение 48 часов с вариантами и ценой.',
    ps3d:'Образец отправляется вам перед оптовым заказом.',
    f_name:'Имя *', f_email:'Email *',
    f_type:'Тип заказа', f_dest:'Страна назначения',
    fo1:'Фото/эскиз → массовое производство', fo2:'Прислать образец → копия и опт',
    fo3:'Только закупка ткани', fo4:'Полный CMT — раскрой, пошив, отделка',
    f_msg:'Опишите ваш запрос',
    f_files:'Референсы / тех. пакет',
    fd_txt:'Перетащите файлы или нажмите', fd_sub:'JPG · PNG · PDF · AI',
    f_gdpr:'Согласен на обратную связь по этому запросу.',
    btn_send:'Отправить запрос',
    ft_tag:'Гуанчжоуский текстильный производитель с 2012 года.',
    ft_rights:'Все права защищены',
    msg_ok:'✓ Отправлено. Ответим в течение 48 часов.',
    msg_err:'Ошибка — напишите на hello@dongxi.io',
    msg_val:'Пожалуйста, заполните имя и email.',
  },
  ar: {
    nav_cat:'الكتالوج', nav_sc:'أعمالنا', nav_inq:'استفسار', nav_shop:'المتجر ↗',
    h_label:'قوانغتشو · مباشرة من المصنع',
    h_line1:'رؤيتك،', h_line2:'مُجسَّدة', h_line3:'بالكامل.',
    cta_inq:'احصل على عرض', cta_shop:'تسوق الآن ↗',
    hero_f1:'عرض خلال 48 ساعة', hero_f2:'الحد الأدنى 50 متر', hero_f3:'شحن عالمي', hero_f4:'تأسس 1995',
    cat_label:'ما نصنعه', cat_h:'كتالوجنا.',
    cat_sub:'اختر مادة لبدء الاستفسار.',
    sc_label:'من مصنعنا', sc_h:'أعمال حديثة.',
    scf_all:'الكل', scf_gar:'ملابس', scf_fab:'أقمشة', scf_emb:'تطريز',
    inq_label:'ابدأ طلبك', inq_h:'احصل على عرض\nخلال 48 ساعة.',
    ps1d:'شارك صورك أو رسوماتك أو عيناتك.',
    ps2d:'نرد خلال 48 ساعة بالخيارات والأسعار.',
    ps3d:'عينة مادية ترسل إليك قبل الإنتاج.',
    f_name:'الاسم *', f_email:'البريد الإلكتروني *',
    f_type:'نوع الطلب', f_dest:'بلد الوجهة',
    fo1:'صورة/رسم → إنتاج بالجملة', fo2:'إرسال عينة → نسخ وإنتاج',
    fo3:'توريد قماش فقط', fo4:'CMT كامل — قص، خياطة، تشطيب',
    f_msg:'أخبرنا بما تحتاجه',
    f_files:'صور مرجعية / حزمة تقنية',
    fd_txt:'اسحب الملفات أو انقر للتصفح', fd_sub:'JPG · PNG · PDF · AI',
    f_gdpr:'أوافق على التواصل بشأن استفساري.',
    btn_send:'إرسال الاستفسار',
    ft_tag:'مصنع نسيج قوانغتشو منذ 2012.',
    ft_rights:'جميع الحقوق محفوظة',
    msg_ok:'✓ تم الإرسال. سنرد خلال 48 ساعة.',
    msg_err:'حدث خطأ — راسلنا على hello@dongxi.io',
    msg_val:'يرجى ملء الاسم والبريد الإلكتروني.',
  },
};

const FABS = [
  'Silk Charmeuse','Silk Chiffon','Silk Satin','Raw Silk',
  'Jacquard Weave','Brocade','Velvet','Corduroy',
  'Cotton Poplin','Cotton Twill','Linen','Denim',
  'Lace','Tulle','Organza','Polyester Taffeta',
  'Recycled Poly','Knit Jersey','Rib Knit',
  'Embroidery Fabric','Burnout Velvet','Devore',
  'Wool Melton','Cashmere Blend',
];

let SHOWCASE = [];
try { SHOWCASE = JSON.parse(showcaseJSON); } catch(e) {}
let curLang = 'en';

// ── NAV SCROLL ───────────────────────────────────────────────
function initNav() {
  const nav = document.getElementById('nav');
  if (!nav) return;
  const update = () => {
    const scrolled = window.scrollY > 60;
    nav.classList.toggle('scrolled', scrolled);
    nav.classList.toggle('on-hero', !scrolled);
  };
  update();
  window.addEventListener('scroll', update, { passive: true });
}

// ── LANGUAGE ─────────────────────────────────────────────────
function applyLang(l) {
  curLang = l;
  const t = T[l] || T.en;
  document.documentElement.dir = l === 'ar' ? 'rtl' : 'ltr';
  document.querySelectorAll('[data-k]').forEach(el => {
    const k = el.dataset.k;
    if (t[k] !== undefined) el.innerHTML = t[k];
  });
  ['langBtn','langBtnM'].forEach(id => {
    const btn = document.getElementById(id);
    if (btn) btn.textContent = l.toUpperCase();
  });
  document.querySelectorAll('.ld-item').forEach(b => {
    b.style.color = b.dataset.l === l ? '#f8f5f1' : '';
  });
}

function initLang() {
  ['langBtn','langBtnM'].forEach((id, i) => {
    const btn = document.getElementById(id);
    const drop = document.getElementById(i === 0 ? 'langDrop' : 'langDropM');
    if (!btn || !drop) return;
    btn.addEventListener('click', e => { e.stopPropagation(); drop.classList.toggle('open'); });
    drop.querySelectorAll('.ld-item').forEach(item => {
      item.addEventListener('click', () => { applyLang(item.dataset.l); drop.classList.remove('open'); });
    });
  });
  document.addEventListener('click', () => {
    document.querySelectorAll('.lang-drop').forEach(d => d.classList.remove('open'));
  });
  const bl = navigator.language.slice(0,2).toLowerCase();
  applyLang(['en','zh','ru','ar'].includes(bl) ? bl : 'en');
}

// ── FABS ─────────────────────────────────────────────────────
function renderFabs() {
  const g = document.getElementById('fabGrid');
  if (!g) return;
  g.innerHTML = FABS.map(f => `<button class="fb" data-fab="${f}">${f}</button>`).join('');
  g.querySelectorAll('.fb').forEach(btn => {
    btn.addEventListener('click', () => {
      const msg = document.getElementById('fMsg');
      if (msg && !msg.value) msg.value = btn.dataset.fab + ' — ';
      document.getElementById('inquiry')?.scrollIntoView({ behavior: 'smooth' });
    });
  });
}

// ── SHOWCASE ─────────────────────────────────────────────────
let scFilter = 'all';
function renderShowcase() {
  const track = document.getElementById('scTrack');
  const empty = document.getElementById('scEmpty');
  if (!track) return;
  const items = scFilter === 'all' ? SHOWCASE : SHOWCASE.filter(s => s.category === scFilter);
  if (!items.length) { track.innerHTML = ''; if (empty) empty.style.display = 'block'; return; }
  if (empty) empty.style.display = 'none';
  track.innerHTML = items.map(s => {
    const imgs = s.images ?? [];
    const slides = imgs.map(src => `<div class="sc-slide"><img src="${src}" alt="${s.title}" loading="lazy"></div>`).join('');
    const dots = imgs.length > 1
      ? `<div class="sc-dots">${imgs.map((_,i) => `<div class="sc-dot${i===0?' on':''}"></div>`).join('')}</div>`
      : '';
    return `<div class="sc-card" data-cat="${s.category}">
      <div class="sc-card-media"><div class="sc-slides">${slides}</div>${dots}</div>
      <div class="sc-info">
        <div class="sc-tag">${s.tag||s.category}</div>
        <div class="sc-title">${s.title}</div>
        ${s.meta?`<div class="sc-meta">${s.meta}</div>`:''}
      </div></div>`;
  }).join('');
  track.querySelectorAll('.sc-card').forEach(card => {
    const sl = card.querySelector('.sc-slides');
    const dots = [...card.querySelectorAll('.sc-dot')];
    if (!sl||!dots.length) return;
    sl.addEventListener('scroll', () => {
      const idx = Math.round(sl.scrollLeft / sl.clientWidth);
      dots.forEach((d,i) => d.classList.toggle('on', i===idx));
    }, { passive:true });
  });
}

function initShowcaseDrag() {
  const wrap = document.querySelector('.sc-track-wrap');
  if (!wrap) return;
  let down=false, sx=0, sl=0;
  wrap.addEventListener('mousedown', e => { down=true; sx=e.pageX-wrap.offsetLeft; sl=wrap.scrollLeft; wrap.style.cursor='grabbing'; });
  wrap.addEventListener('mouseleave', () => { down=false; wrap.style.cursor='grab'; });
  wrap.addEventListener('mouseup',   () => { down=false; wrap.style.cursor='grab'; });
  wrap.addEventListener('mousemove', e => { if(!down) return; e.preventDefault(); wrap.scrollLeft=sl-(e.pageX-wrap.offsetLeft-sx); });
}

// ── FORM ─────────────────────────────────────────────────────
function initForm() {
  const drop  = document.getElementById('fileDrop');
  const input = document.getElementById('fFiles');
  const list  = document.getElementById('fileList');
  if (drop && input && list) {
    drop.addEventListener('click', () => input.click());
    drop.addEventListener('dragover', e => { e.preventDefault(); drop.style.borderColor='rgba(248,245,241,.4)'; });
    drop.addEventListener('dragleave', () => { drop.style.borderColor=''; });
    drop.addEventListener('drop', e => {
      e.preventDefault(); drop.style.borderColor='';
      const dt = new DataTransfer();
      [...(input.files??[])].forEach(f=>dt.items.add(f));
      [...(e.dataTransfer?.files??[])].forEach(f=>dt.items.add(f));
      input.files=dt.files; renderFiles();
    });
    input.addEventListener('change', renderFiles);
    function renderFiles() {
      list.innerHTML = [...(input.files??[])].map(f=>`<div class="fi-chip">${f.name}</div>`).join('');
    }
  }

  document.getElementById('iqForm')?.addEventListener('submit', async e => {
    e.preventDefault();
    const t = T[curLang];
    const name  = document.getElementById('fName').value.trim();
    const email = document.getElementById('fEmail').value.trim();
    const gdpr  = document.getElementById('fGdpr').checked;
    if (!name||!email||!gdpr) { showMsg(t.msg_val,'err'); return; }
    const btn = document.getElementById('subBtn');
    if (btn) { btn.disabled=true; btn.textContent='…'; }
    const fd = new FormData();
    fd.append('access_key','e9868ff6-1838-4053-8bf9-5f701496acfa');
    fd.append('subject',`New DONGXI Inquiry from ${name}`);
    fd.append('name', name);
    fd.append('email', email);
    fd.append('orderType', document.querySelector('input[name=orderType]:checked')?.value||'');
    fd.append('destination', document.getElementById('fDest')?.value||'');
    fd.append('message', document.getElementById('fMsg')?.value||'');
    const files = document.getElementById('fFiles')?.files??[];
    for (let i=0;i<files.length;i++) fd.append('attachment'+i, files[i], files[i].name);
    try {
      const res  = await fetch('https://api.web3forms.com/submit',{method:'POST',body:fd});
      const json = await res.json();
      showMsg(json.success ? t.msg_ok : t.msg_err, json.success ? 'ok' : 'err');
      if (json.success) { document.getElementById('iqForm').reset(); list&&(list.innerHTML=''); }
    } catch { showMsg(t.msg_err,'err'); }
    finally { if(btn){btn.disabled=false;btn.textContent=T[curLang].btn_send;} }
  });
}

function showMsg(msg, cls) {
  const el = document.getElementById('iqMsg');
  if (!el) return;
  el.textContent = msg;
  el.className = 'iq-msg ' + cls;
}

// ── INIT ─────────────────────────────────────────────────────
document.addEventListener('DOMContentLoaded', () => {
  initNav();
  initLang();
  renderFabs();
  renderShowcase();
  initShowcaseDrag();
  initForm();

  document.querySelectorAll('.scf').forEach(btn => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.scf').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      scFilter = btn.dataset.f ?? 'all';
      renderShowcase();
    });
  });
});
</script>
